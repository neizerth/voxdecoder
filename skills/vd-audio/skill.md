# Audio processing

Use VoxDecoder Runtime via MCP to plan and run audio Jobs (`plan.audio` / process audio).

Prefer MCP tools over inventing file paths. Ask for confirmation before `execute: true`.
