[package]
name = "vd-classify"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Meeting-input filename classification heuristics (name / mix / gender)"
publish = false

[lib]
name = "vd_classify"
path = "src/lib.rs"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
