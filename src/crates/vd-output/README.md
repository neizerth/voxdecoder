# vd-output

Output path resolution for transcript CLIs.

## Owns

- `-o` / `-d` / `--in-place` / `--overwrite`
- Default `{stem}.fixed.{ext}`

Depends on [`vd-artifact`](../vd-artifact/) for `ArtifactType`.

```bash
cargo test -p vd-output
```
