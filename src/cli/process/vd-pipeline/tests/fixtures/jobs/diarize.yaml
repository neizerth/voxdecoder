version: 1
name: diarize-stub
input:
  audio: meeting.wav
steps:
  - use: diarize
    id: timeline
    options:
      backend:
        provider: stub
        model: deterministic-v1
      overwrite: true
